/* @mixin forLargeScreens($media) {
  @media (min-width: $media) {
    @content;
  }
} */

/* @mixin setFlexDisplay($direction, $align, $justify) {
  display: flex;
  flex-direction: $direction;
  align-items: $align;
  justify-content: $justify;
}

@mixin setImageDisplay($maxWidth, $height) {
  max-width: $maxWidth;
  height: $height;
} */

/* @mixin setLinkDark() {
  @extend %paletteDark;
    text-decoration: none;
    transition: color 0.5s;
    border: none;
    
    &:active {
        color: $color_text-active;
    }
    &:hover {
        color: $color_text-contrast;
        cursor: pointer;
    }
} */

/* =================== */

/* @mixin btnEffect {
    border: {
        color: $customOrange;
        width: 1px;
        style: solid;
    }
    padding: 10px;
    background-color: $customLightGrey;
    margin: 35px;
    &:hover {
        padding: 14px;
        background-color: $customGrey;
        color: white;
        margin: 30px;
        border: {
            width: 2px;
        }
    }
} */
/* @mixin shadowEffect {
    box-shadow: 5px 5px 10px $customGrey;
}
@mixin flexCenteredColumn {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
} */
/* @mixin showHide {
    .show {
        display: block;
    }
    .hide {
        display: none;
    }
}
@mixin flexCenteredRow {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
}
@mixin flexSpaceBetweenColumn {
    display: flex;
    flex-direction: column;
    align-items: space-between;
}
@mixin flex-row {
    display: flex;
    flex-direction: row;
} */